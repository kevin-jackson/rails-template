= simple_form_for [:admin, @client], wrapper: "horizontal", html: { class: "form-horizontal"}, url: url do |f|
  = f.error_notification

  .form-inputs
    = f.input :name

  .form-actions
    = f.button :submit
    %span.or or
    = link_to "Cancel", admin_clients_path